#!/usr/bin/env python3
"""Thin launcher for the layout serving app (kept for direct ``bin/`` invocation).

The real service is ``ddp_layout.layout_service.LayoutMicroservice`` (scope-aware, ``/sly/...``);
prefer the installed console script ``ddpa_layout_serve`` (declared in pyproject.toml).
The superseded whole-DB service is ``bin/ddpa_layout_serve_legacy``.
"""
from ddp_layout.scripts.ddp_layout_online import main

if __name__ == "__main__":
    main()
