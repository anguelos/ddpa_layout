#!/usr/bin/env python3
"""Thin launcher for the layout serving app (kept for direct ``bin/`` invocation).

The real service is ``ddp_layout.layout_service.LayoutMicroservice``; prefer the installed
console script ``ddpa_layout_serve`` (declared in pyproject.toml). This file used to be a
byte-for-byte copy of the old procedural online script -- that is gone; it now just delegates.
"""
from ddp_layout.scripts.ddp_layout_online import main

if __name__ == "__main__":
    main()
